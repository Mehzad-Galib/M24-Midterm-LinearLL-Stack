#include <bits/stdc++.h>
using namespace std;

int main()
{
    int dim;
    cin >> dim;
    int a[dim][dim];

    for (int i = 0; i < dim; i++)
    {
        for (int j = 0; j < dim; j++)
        {
            cin >> a[i][j];
        }
    }
}